{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install --no-cache-dir -r requirements.txt && python -c \"import gdown; import zipfile; import os; import sys; MODEL_DIR='model_saved_tf'; MODEL_ZIP='model_saved_tf.zip'; print('📥 Downloading Harvard model during build...'); try: gdown.download('https://drive.google.com/uc?id=12wNpYBz3j5mP9mt6S_ZH4k0sI6dVDeVV', MODEL_ZIP, quiet=False); print('📦 Extracting Harvard model...'); zipfile.ZipFile(MODEL_ZIP, 'r').extractall('.'); os.remove(MODEL_ZIP); print('✅ Harvard model ready for deployment'); except Exception as e: print(f'❌ Model download failed: {e}'); sys.exit(1)\"",
    "watchPatterns": ["**/*.py", "**/*.tsx", "**/*.ts", "**/*.js", "**/*.json"]
  },
  "deploy": {
    "startCommand": "python main.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "on_failure",
    "numReplicas": 1,
    "healthcheckInterval": 60
  },
  "environments": {
    "production": {
      "variables": {
        "RAILWAY_ENVIRONMENT": "production",
        "LOAD_HARVARD_MODEL": "true",
        "ENABLE_DEEPFACE": "false",
        "TF_CPP_MIN_LOG_LEVEL": "3",
        "TF_ENABLE_ONEDNN_OPTS": "0",
        "CUDA_VISIBLE_DEVICES": "-1",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
} 